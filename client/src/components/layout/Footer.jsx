import React from 'react';
import './Footer.css';

const Footer = () => {
  return (
    <footer className="footer">
      {/* Footer is mainly handled in Contact section for this specific design layout, 
          but keeping this component for structural completeness if more elements are needed */}
    </footer>
  );
};

export default Footer;
